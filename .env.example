# Base URL the browser uses to reach the FastAPI backend (via Next.js rewrites)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME="Autonomous Multi-Region Payment & Identity Settlement Network"
